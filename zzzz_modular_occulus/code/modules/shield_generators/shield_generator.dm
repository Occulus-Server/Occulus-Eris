// just some simple stuff to make the shield generator foolproof (and also more convenient to use)

/obj/machinery/power/shield_generator
	default_modes = list(MODEFLAG_HYPERKINETIC, MODEFLAG_HULL, MODEFLAG_MULTIZ)
